# property-viewer-mvp — Implementation Tickets

---

# 01 — Project scaffold: Next.js app with TypeScript, Tailwind, and shadcn/ui

**What to build:** A working Next.js 14+ App Router project with TypeScript, Tailwind CSS, and shadcn/ui installed and configured. The app boots with a blank homepage at `http://localhost:3000`.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Initialize Next.js project with `create-next-app` using TypeScript and Tailwind
- [ ] Install shadcn/ui and configure components
- [ ] Set up Tailwind config with Stripe design tokens (colors, fonts)
- [ ] Verify `npm run dev` starts the app with a blank page
- [ ] Commit: `feat: scaffold Next.js project with Tailwind and shadcn/ui`

---

# 02 — FastAPI RAG wrapper: /search and /properties endpoints

**What to build:** A FastAPI application that wraps the existing `rag_pipeline.py` functions. Exposes `/api/search?q=<query>` (returns JSON property list) and `/api/properties` (returns all properties with optional filters for price, bedrooms, property type). Reuses the existing ChromaDB collection and `properties_data.json` — no new scraping.

**Blocked by:** Ticket 01 (frontend needs an API target to connect to)

**Status:** blocked-by-01

- [ ] Create `api/main.py` with FastAPI app
- [ ] Import and wrap `search_properties()` and `build_vector_store()` from `rag_pipeline.py`
- [ ] Implement `/api/search` endpoint accepting `q` query param, returning JSON list of properties with id, title, price, location, bedrooms, bathrooms, size, similarity score
- [ ] Implement `/api/properties` endpoint with optional `min_price`, `max_price`, `bedrooms`, `property_type` query params
- [ ] Test endpoints with `curl` or HTTPie
- [ ] Commit: `feat: add FastAPI RAG wrapper endpoints`

---

# 03 — Property card component (Explore surface)

**What to build:** A reusable `PropertyCard` React component that renders a property listing with thumbnail carousel, title, location, price, bedrooms/bathrooms/size badges, and a share button. Styled with Stripe design tokens but using the blue/white/gray palette from youroverseashome.com. The card is clickable and links to a detail page.

**Blocked by:** Ticket 01 (needs the project scaffold)

**Status:** blocked-by-01

- [ ] Create `PropertyCard` component with: image carousel (single image for MVP), title, location, price (bold), specs row (beds/baths/sqm), share button
- [ ] Apply Stripe tokens: white background, 4px radius, blue-tinted shadow (`rgba(50,50,93,0.25)`), Source Sans 3 font, 16px body, 22px card title weight 300
- [ ] Apply youroverseashome.com palette: `blue-600` for prices and accents, `white` for card surface, `gray-50` for page background, `gray-700` for body text
- [ ] Make responsive: single column on mobile, 2-col on tablet, 3-col on desktop
- [ ] Add hover state (shadow intensifies)
- [ ] Commit: `feat: add PropertyCard component`

---

# 04 — Homepage with property grid and search bar (Explore surface)

**What to build:** The homepage at `/` that renders a sticky header (logo + nav + search bar), a filter bar (price range, sort by recently added/price), and a grid of `PropertyCard` components. The search bar accepts natural language queries and hits the `/api/search` endpoint. Pagination at the bottom.

**Blocked by:** Tickets 02 and 03

**Status:** blocked-by-02, blocked-by-03

- [ ] Build sticky header with logo (text-based, blue-600), nav links, and search input
- [ ] Style search input: white bg, gray-300 ring, blue-500 focus ring, 10px left padding with search icon
- [ ] Build filter bar: sort dropdown (Recently Added, Price Low-High, Price High-Low), price range inputs
- [ ] Render PropertyCard grid (3-col desktop, 2-col tablet, 1-col mobile)
- [ ] Implement client-side search: type → debounce → call `/api/search` → re-render grid
- [ ] Implement pagination (20 per page) with Previous/Next buttons
- [ ] Wire up filter + sort controls to `/api/properties` endpoint
- [ ] Commit: `feat: build homepage with search, filters, and property grid`

---

# 05 — Property detail page (Inspect surface)

**What to build:** A detail page at `/properties/[id]` that shows the full property information: large thumbnail, title, location, price, all specs (bedrooms, bathrooms, size, property type), full description, and a share button. Uses the Command/Inspect surface archetype — focused, dense, no distractions.

**Blocked by:** Ticket 03 (needs PropertyCard click handler and routing)

**Status:** blocked-by-03

- [ ] Set up Next.js dynamic route at `app/properties/[id]/page.tsx`
- [ ] Fetch property data from `/api/properties` or `/api/search` by ID
- [ ] Render: large hero image, title (weight 300, 32px), location, price (blue-600, bold), spec badges in a row, full description text
- [ ] Add share button that copies the URL to clipboard
- [ ] Apply Stripe surface treatment: white card, blue-tinted shadow, 8px radius for hero, clean typography hierarchy
- [ ] Add back-to-search navigation
- [ ] Commit: `feat: add property detail page`

---

# 06 — Vercel deployment config and CI

**What to build:** `vercel.json` configuration, `Dockerfile` for the FastAPI backend, CI workflow (optional but recommended), and a verified deploy to Vercel. The frontend deploys as a standard Next.js Vercel project; the FastAPI backend runs as a Vercel Serverless Function or alternative (container/Cloud Run if Vercel limits are hit).

**Blocked by:** Tickets 04 and 05

**Status:** blocked-by-04, blocked-by-05

- [ ] Create `vercel.json` with routing rules for Next.js and API rewrites to FastAPI
- [ ] Create `Dockerfile` for FastAPI backend (or integrate into Next.js API routes for simplicity)
- [ ] Create `README.md` with local dev setup instructions and Vercel deploy instructions
- [ ] Deploy to Vercel and verify homepage loads, search works, detail pages render
- [ ] Commit: `feat: add Vercel deployment config and verify deploy`

---

# 07 — Visual polish: design token pass and responsive refinements

**What to build:** A design pass applying the Stripe + youroverseashome.com design system consistently. Verify responsive behavior at all breakpoints. Run the claude-design slop diagnostic on the artifact and fix any issues (center stack, generic cards, wrong surface, decorative elements without purpose).

**Blocked by:** Ticket 06 (needs deployed app to verify)

**Status:** blocked-by-06

- [ ] Run claude-design slop self-audit: score the app out of 10, identify tells
- [ ] Fix compositional issues (re-layout) before cosmetic ones (recolor)
- [ ] Verify responsive at 320px, 768px, 1024px, 1440px breakpoints
- [ ] Ensure touch targets are >= 44px on mobile
- [ ] Verify typography: weight 300 headlines, Source Sans 3, negative letter-spacing at display sizes
- [ ] Verify shadow system uses blue-tinted shadows
- [ ] Verify no pure black (`#000000`) — use `#061b31` for headings
- [ ] Commit: `style: apply design token pass and responsive refinements`

---

# 08 — Visual comparison artifact and design handoff

**What to build:** A local HTML artifact (using claude-design + popular-web-designs Stripe template) that shows the design system applied to the property search page, for stakeholder review or design handoff. Includes the blue/white/gray palette and improved card layouts vs. the reference site.

**Blocked by:** Ticket 07 (needs polished design to reference)

**Status:** blocked-by-07

- [ ] Generate a self-contained HTML file: `docs/design-property-search.html`
- [ ] Apply Stripe tokens with youroverseashome.com blue palette
- [ ] Show property grid, search bar, filter bar, and detail page as a prototype
- [ ] Include Tweaks panel (theme mode, density, accent color)
- [ ] Run browser_vision to verify visual accuracy
- [ ] Commit: `docs: add design artifact for stakeholder review`
